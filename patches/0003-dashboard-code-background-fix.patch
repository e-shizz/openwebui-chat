diff --git a/web/src/index.css b/web/src/index.css
index e9818174..56823a79 100644
--- a/web/src/index.css
+++ b/web/src/index.css
@@ -120,7 +120,24 @@ code, kbd, pre, samp, .font-mono, .font-mono-ui {
 /* Nousnet's hermes-agent layout bumps `small` and `code` to readable
    dashboard sizes. Keep in sync. */
 small { font-size: 1.0625rem; }
-code { font-size: 0.875rem; }
+
+/* Fix: decouple <code> background from the animated --selection-bg
+   cycled by @nous-research/ui's SelectionSwitcher. Without this,
+   every inline code block on the page changes color whenever the
+   user selects text or presses Ctrl+A.
+   Scope to inline code only (not inside hljs code blocks) so
+   syntax highlighting in code fences is preserved. */
+code:not(pre.hljs code) {
+  font-size: 0.875rem;
+  background: var(--midground);
+  color: var(--background);
+}
+
+/* Code blocks: transparent background so the container's glass
+   effect (bg-black/20) shows through. hljs theme colors preserved. */
+pre.hljs, pre.hljs code {
+  background: transparent !important;
+}
 
 /* Shadcn-compat tokens.
    The dashboard's page code predates the Nous DS and uses shadcn-style
